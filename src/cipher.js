window.cipher = {
  encode: () => {
    /* Acá va tu código */
  },
  
  let caesarCipher = function(){
  let x = document.getElementById("ingresarMsj").value;
  let displacement=parseInt(document.getElementById("key").value);

  for (i = 0; i < x.lenght; i++);
  n = x.charCodeAt(i);

  if (n = x >= 65 && n <= 90 {
  out=String.fromCharCode((n - 65 + displacement) % 26 + 65);

  } else if (n >= 97 && n <= 122) { 

  }


}



/*
  decode: () => {
    /* Acá va tu código */
  }


